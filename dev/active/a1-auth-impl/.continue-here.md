---
context: dev-docs
phase: A1 (closed locally) — push gate blocked
status: paused_ci_fail
last_updated: 2026-04-26T23:30:00Z
---

# A1 Close Push — CI Fail (gradlew exec bit)

## 1차 컨텍스트
1. `.planning/HANDOFF.json` — 구조화된 상태, blocker `ci-gradlew-perm`, fix 1-line
2. **CI 로그**: https://github.com/xorrbss/IbizDrive/actions/runs/24956173267/job/73074697584
   - 실패 라인: `./gradlew: Permission denied` → `Process completed with exit code 126`
   - 실패 시점: `Run ./gradlew test --no-daemon` 즉시 (~10초)

## 진단 (확정)
- **원인**: `backend/gradlew`의 git mode가 `100644` (rw-r--r--)이어야 할 자리에 `100644` 그대로 (Windows에서 작성/commit되어 exec bit 0). Linux runner가 실행 시도 → permission denied.
- **본 세션 신규 코드와 무관**: A1.0~A1.6 production/test 코드 156 tests 모두 로컬 PASS, 컴파일 OK. 단순히 Linux 파일 권한 문제.
- **frontend (vitest) PASS** — frontend 영향 0.

## Fix (다음 세션 첫 명령)
```bash
git update-index --chmod=+x backend/gradlew
git diff --cached  # mode change 확인 (100644 → 100755)
git commit -m "fix(ci): gradlew exec bit for linux runner (chmod +x)

Windows commit으로 backend/gradlew의 exec bit 누락. Linux runner가
\`./gradlew test --no-daemon\` 호출 시 'Permission denied' (exit 126).

git update-index --chmod=+x로 git tree에 0755 모드 명시.
"
git push
gh pr checks 1 --watch
```

## CI 그린 후 잔여 검증
- backend (junit) PASS — 컴파일 + 152 production tests + Testcontainers `AuthScenarioIntegrationTest` (ubuntu-latest, Docker 가용) 실제 실행 → audit accepted-deviation #3 close
- frontend (vitest) — 이미 PASS 확인됨

## 추가 fail 시 (보호망)
- Testcontainers Docker 셋업 누락 — Linux runner에 Docker 데몬 가용 여부 확인 (`docker info`)
- Postgres 15-alpine 이미지 pull 시간 — timeout 발생 시 `@Timeout` 또는 `testcontainers.reuse.enable=true`
- 이 경우 별도 handoff (본 fix와 분리 commit)

## 본 세션 산출 (commit 8edbefe..80a40e2 push 완료)
- A1.6: `SessionValidityFilter` + `application.yml PT30M` (4aa372c) — must-fix #1 close
- A1 close: audit RESOLVED + progress 3 블록 + HANDOFF/.continue-here 정리 (80a40e2)
- PR #1 edit: title `feat: M1~M16 + A1 backend auth`, body에 A1 섹션 prepend
- 156 tests local PASS (152 + 4 Docker SKIP, 0 fail)

## next_action
`/clear` → `/gsd-resume-work` → fix 1-line → push → CI watch.

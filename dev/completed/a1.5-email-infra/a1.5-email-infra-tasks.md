# a1.5-email-infra — Tasks

Last Updated: 2026-05-02

## Phase 상태

- P0 (worktree+dev-docs bootstrap): **in_progress**
- P1 (Spring Mail + EmailService): pending
- P2 (V8 password_reset_tokens): pending
- P3 (forgot endpoint TDD): pending
- P4 (reset endpoint TDD): pending
- P5 (change endpoint TDD): pending
- P6 (frontend pages): pending
- P7 (closure: ADR + docs sync + PR): pending

### P0 worktree + dev-docs

- [x] worktree `C:/project/IbizDrive/.claude/worktrees/a1.5-email-infra` 생성, branch `wip/a1.5-email-infra` (base `wip/auth-pages`)
- [x] dev/active/a1.5-email-infra/ 3파일 생성
- [x] dev/process/a1.5-email-infra.md ownership 파일 생성
- [ ] bootstrap commit

### P1 Spring Mail + EmailService 추상화

- [ ] `backend/build.gradle.kts` — `implementation("org.springframework.boot:spring-boot-starter-mail")` 추가
- [ ] `backend/src/main/java/com/ibizdrive/email/EmailService.java` — 인터페이스 (`send(to, subject, body)`)
- [ ] `backend/src/main/java/com/ibizdrive/email/ConsoleEmailService.java` — `@Service @Profile("!prod")` 또는 `@ConditionalOnMissingBean` (default)
- [ ] `backend/src/main/java/com/ibizdrive/email/SmtpEmailService.java` — `@Service @Profile("prod")` JavaMailSender 사용
- [ ] `backend/src/main/resources/application-prod.yml` — `spring.mail.*` placeholder

#### 작업 전 필독
- `backend/src/main/java/com/ibizdrive/auth/SignupService.java` (이벤트 publish 패턴 참조)
- `backend/build.gradle.kts` (현재 의존성)
- `backend/src/main/resources/application.yml` (프로파일 분리 기존 패턴)

#### 검증 참조
- `cd backend && ./gradlew compileJava`
- `cd backend && ./gradlew test --tests *EmailService*`(존재 시)

### P2 V8 password_reset_tokens

- [ ] `backend/src/main/resources/db/migration/V8__password_reset_tokens.sql`:
  - id BIGSERIAL PK
  - user_id BIGINT NOT NULL FK users(id)
  - token_hash VARCHAR(64) NOT NULL UNIQUE — SHA-256 hex
  - expires_at TIMESTAMPTZ NOT NULL
  - used_at TIMESTAMPTZ NULL
  - created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
  - INDEX (user_id), INDEX (expires_at) WHERE used_at IS NULL
- [ ] `backend/src/main/java/com/ibizdrive/auth/password/PasswordResetToken.java` — `@Entity` + 필드 매핑
- [ ] `backend/src/main/java/com/ibizdrive/auth/password/PasswordResetTokenRepository.java` — `JpaRepository<PasswordResetToken, Long>` + `findByTokenHash(String)`

#### 작업 전 필독
- `backend/src/main/resources/db/migration/V2__users_auth.sql` (users 테이블 컬럼)
- 기존 V*.sql 패턴(인덱스 명명, TIMESTAMPTZ 사용)
- `backend/src/main/java/com/ibizdrive/user/User.java` (엔티티 패턴)

#### 검증 참조
- `./gradlew test --tests *FlywayMigration*`(존재 시), 부재 시 부트 시 `flyway migrate` 정상 통과

### P3 POST /api/auth/password/forgot (TDD)

- [ ] `PasswordControllerTest` RED:
  - (a) 가입자 → 200, 토큰 1건 INSERT (token_hash != null), email 1회 send
  - (b) 미가입자 → 200, 토큰 0건, email 0회 send
  - (c) 응답 body는 `{ "message": "..." }` 동일
- [ ] `PasswordController.forgot()` + `PasswordResetService.requestReset(email)` GREEN:
  - 평문 토큰: `UUID.randomUUID() + UUID.randomUUID()` (랜덤성 충분, 64자)
  - DB엔 `sha256(token)` 저장, 이메일엔 평문 + 링크 `${app.url}/reset-password?token=${token}`
  - 30분 TTL: `expires_at = now() + 30min`
  - audit 이벤트 `password.forgot`(가입자만)
- [ ] `SecurityConfig`에 `/api/auth/password/forgot` permitAll + CSRF 면제

#### 작업 전 필독
- `backend/src/main/java/com/ibizdrive/auth/SignupService.java` (UserRegisteredEvent 패턴, BCrypt 사용)
- `backend/src/main/java/com/ibizdrive/config/SecurityConfig.java` (signup permitAll 패턴)
- `backend/src/main/java/com/ibizdrive/audit/` (audit 이벤트 publish 패턴)

#### 검증 참조
- `./gradlew test --tests *PasswordController*`
- `./gradlew test --tests *PasswordResetService*`

### P4 POST /api/auth/password/reset (TDD)

- [ ] `PasswordControllerTest` RED:
  - (a) 유효 토큰 + 정책 만족 PW → 200, users.password_hash 갱신, used_at != null, 모든 세션 invalidate
  - (b) 만료 토큰 → 400 INVALID_TOKEN
  - (c) 사용된(used_at != null) 토큰 → 400 INVALID_TOKEN
  - (d) 존재하지 않는 토큰 → 400 INVALID_TOKEN
  - (e) PW 정책 위반 → 400 INVALID_PASSWORD
- [ ] `PasswordController.reset()` + `PasswordResetService.reset(token, newPassword)` GREEN:
  - 트랜잭션 내 `findByTokenHash(sha256(token))` SELECT FOR UPDATE
  - PW 정책 검증 (분리: `PasswordPolicyValidator`)
  - users.password_hash = BCrypt.hash(newPassword), used_at = now()
  - `JdbcIndexedSessionRepository.findByPrincipalName(email)` → 각 세션 delete
  - audit `password.reset`
- [ ] `SecurityConfig`에 `/api/auth/password/reset` permitAll + CSRF 면제
- [ ] `PasswordPolicyValidator` 신규 — SignupService에서 사용 중인 PW 정책을 추출

#### 작업 전 필독
- `backend/src/main/java/com/ibizdrive/auth/SignupService.java` (BCrypt 사용 패턴, PW 검증 어디 있는지)
- Spring Session JDBC `JdbcIndexedSessionRepository` 사용법

#### 검증 참조
- `./gradlew test --tests *PasswordController*`
- `./gradlew test --tests *PasswordResetService*`
- `./gradlew test --tests *PasswordPolicyValidator*`

### P5 POST /api/auth/password/change (TDD)

- [ ] `PasswordControllerTest` RED:
  - (a) 인증된 사용자 + 현재 PW 일치 + 정책 만족 신규 PW → 200, 갱신, 다른 세션 invalidate(현재 세션 유지)
  - (b) 현재 PW 불일치 → 400 INVALID_CREDENTIALS
  - (c) 신규 PW 정책 위반 → 400 INVALID_PASSWORD
  - (d) 미인증 → 401
- [ ] `PasswordController.change()` + 서비스 GREEN:
  - 현재 세션 ID는 유지 — `JdbcIndexedSessionRepository.findByPrincipalName(email)`에서 `request.getSession().getId()` 외 항목만 delete
  - audit `password.change`

#### 작업 전 필독
- 위 P4 작업 결과(서비스/컨트롤러 시그니처)
- `backend/src/main/java/com/ibizdrive/user/IbizDriveUserDetails.java`

#### 검증 참조
- `./gradlew test --tests *PasswordController*`

### P6 Frontend pages

- [ ] `frontend/src/app/forgot-password/page.tsx` — 이메일 input + 제출 → POST `/api/auth/password/forgot` → 결과 메시지(가입/미가입 동일)
- [ ] `frontend/src/app/reset-password/page.tsx` — `useSearchParams().get('token')` + 새 PW input → POST `/api/auth/password/reset` → 성공 시 `router.replace('/login')`
- [ ] `frontend/src/app/account/password/page.tsx` — `<AuthGuard>` 안. 현재 PW + 새 PW → POST `/api/auth/password/change`
- [ ] `frontend/src/components/auth/UserMenu.tsx` — "비밀번호 변경" Link `href="/account/password"` 추가
- [ ] `frontend/src/app/login/page.tsx` — "비밀번호 분실" 링크 `href="/forgot-password"` 추가

#### 작업 전 필독
- `frontend/src/app/login/page.tsx`, `frontend/src/app/signup/page.tsx` (form 패턴)
- `frontend/src/components/auth/UserMenu.tsx` (현재 구현)
- `frontend/src/lib/api/auth.ts` 또는 useLogin/useSignup hooks (호출 패턴)

#### 검증 참조
- `cd frontend && npm run typecheck`
- 수동: dev 서버에서 `/forgot-password` 흐름 확인

### P7 closure

- [ ] ADR — EmailService 추상화 + dev/prod 분리 정책
- [ ] ADR — 비밀번호 재설정 토큰 SHA-256 해시 저장 + 30min TTL + 1회용
- [ ] `docs/03-security-compliance.md` §2.7 비밀번호 재설정 흐름 작성
- [ ] `docs/03-security-compliance.md` §4 audit 이벤트 추가(`password.forgot`, `password.reset`, `password.change`)
- [ ] `docs/02-backend-data-model.md` §7 API 추가
- [ ] `docs/progress.md` — a1.5-email-infra closure 블록
- [ ] dev/active/a1.5-email-infra → dev/completed/a1.5-email-infra 이동
- [ ] dev/process/a1.5-email-infra.md 삭제
- [ ] 스택드 PR open: base = `wip/auth-pages`(또는 master 머지 후 master)

#### 문서 반영
- ADR은 `docs/00-overview.md §5` 섹션에 추가
- API 스펙은 docs/02 §7.4 인증 섹션 또는 신규 §7.5 비밀번호 섹션
- audit 이벤트는 docs/03 §4 표에 행 추가

# audit export cap 외부화 (`@ConfigurationProperties`)

- 시작: 2026-05-08
- 작은 트랙 — README + commit message로 통합 (spec/plan/tasks 별도 없음).
- `EXPORT_ROW_CAP` 하드코딩 → `app.audit.export.row-cap` (application.yml).
- 머지 후 `dev/completed/`로 이동.
